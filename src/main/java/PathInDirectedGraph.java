public class PathInDirectedGraph {
    public int solve(int A, ArrayList<ArrayList<Integer>> B) {
        ArrayList<Set<Integer>> egdes = new List<>(A);
        for(int i=0; i<A; i++){
            edges.Add(new Hashset<Integetr>());
        }
    }
}
